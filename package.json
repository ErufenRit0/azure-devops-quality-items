{
  "name": "azure-devops-quality-items",
  "version": "0.1.0",
  "description": "Azure DevOps extension that adds a Quality Item work item type with a custom form panel, a backlog-style hub, and optional AI-assisted classification.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && webpack --mode production",
    "watch": "webpack --mode development --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "package": "npm run build && tfx extension create --manifest-globs vss-extension.json --output-path dist/quality-items.vsix"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "copy-webpack-plugin": "^12.0.0",
    "eslint": "^8.57.0",
    "rimraf": "^5.0.5",
    "tfx-cli": "^0.17.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "azure-devops-extension-api": "^4.240.0",
    "azure-devops-extension-sdk": "^4.0.2"
  }
}
